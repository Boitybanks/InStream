[project]
name = "instream-workflow"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "instream-shared",
    "instream-confidence",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["instream_workflow"]
